import Alpine from 'alpinejs';

declare global {
  interface Window {
    Alpine: typeof Alpine;
    app: () => object;
  }
}

window.app = () => ({
  message: 'Welcome to {{.AppName}}',
  init() {
    console.log('{{.AppName}} frontend initialized');
  },
});

window.Alpine = Alpine;
Alpine.start();
